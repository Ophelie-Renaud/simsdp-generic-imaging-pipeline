/**
* @file /Cluster_sub1_urc_51.c/h
* @generated by CodegenScape
* @date Tue Dec 10 11:11:37 CET 2024
*/

#include "Cluster_sub1_urc_51.h"

 void Cluster_sub1_urc_51Init(){
}
void Cluster_sub1_urc_51(PRECISION2 *delta_psi_1_gains_gains,PRECISION2 *delta_psi_1_delta_correct_to_finegrid_gains_in_if_gains_in){

// buffer declaration

PRECISION2 delta_psi_1_gains_if_gains__delta_psi_1_delta_gains_gains[512];

PRECISION2 delta_psi_1_delta_gains_if_gains__delta_psi_1_delta_correct_to_finegrid_gains_in_gains_in[512];

// body 
//delta_psi_1_gains
memcpy(delta_psi_1_gains_if_gains__delta_psi_1_delta_gains_gains + 0,delta_psi_1_gains_gains + 0,512*sizeof(PRECISION2));
//delta_psi_1_delta_gains
memcpy(delta_psi_1_delta_gains_if_gains__delta_psi_1_delta_correct_to_finegrid_gains_in_gains_in + 0,delta_psi_1_gains_if_gains__delta_psi_1_delta_gains_gains + 0,512*sizeof(PRECISION2));
//delta_psi_1_delta_correct_to_finegrid_gains_in
memcpy(delta_psi_1_delta_correct_to_finegrid_gains_in_if_gains_in + 0,delta_psi_1_delta_gains_if_gains__delta_psi_1_delta_correct_to_finegrid_gains_in_gains_in + 0,512*sizeof(PRECISION2));


// free buffer
}
